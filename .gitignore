# bv (beads viewer) local config and caches
.bv/

# app build artifacts
app/node_modules/
app/.svelte-kit/
app/build/
app/dist/
app/src-tauri/target/

# spike build artifacts
spikes/stdio_rpc/target/

# daemon build artifacts
daemon/target/
*.snap.new

# beads local runtime files
.beads/.local_version
.beads/daemon.log
.beads/daemon-error
.beads/*.pid

# local build outputs
a.out
